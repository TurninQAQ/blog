---
phase: 01-visual-foundation-and-public-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - VIS-03
  - QUAL-02
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - "Every current npm release flagged SUS in research is verified individually before installation."
    - "The human approval covers the exact pinned package set used by the scaffold and browser verification plans."
    - "No package-manager install runs before the approval checkpoint is recorded."
  artifacts:
    - path: ".planning/phases/01-visual-foundation-and-public-shell/01-01-SUMMARY.md"
      provides: "Recorded package verification output and approval signal for the pinned npm set"
      contains: "package approval"
  key_links:
    - from: ".planning/phases/01-visual-foundation-and-public-shell/01-RESEARCH.md"
      to: ".planning/phases/01-visual-foundation-and-public-shell/01-01-SUMMARY.md"
      via: "Package Legitimacy Audit drives per-package verification and approval evidence"
      pattern: "Package Legitimacy Audit"
---

<objective>
Secure the package supply-chain gate for Phase 1 before any scaffold or install command runs.

Purpose: The research audit flagged the current release line for the scaffold, UI, and browser-test packages as `SUS` because the versions are very recent. This plan records per-package verification and human approval before implementation plans install them.
Output: A package approval checkpoint and `.planning/phases/01-visual-foundation-and-public-shell/01-01-SUMMARY.md`.
</objective>

## Phase Goal

**As a** public visitor, **I want to** open a distinctive technical-lab public shell with clear routes into the owner's content areas, **so that** I can understand the programmer identity while the site stays readable and responsive.

## Artifacts This Phase Produces (Plan 01-01 Slice)

- `.planning/phases/01-visual-foundation-and-public-shell/01-01-SUMMARY.md`: verification output and approval signal for the exact package set.
- No app scaffold, dependency install, route, component, or generated lockfile changes happen in this plan before approval.

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-and-public-shell/01-CONTEXT.md
@.planning/phases/01-visual-foundation-and-public-shell/01-RESEARCH.md
@.planning/phases/01-visual-foundation-and-public-shell/01-PATTERNS.md
@.planning/phases/01-visual-foundation-and-public-shell/01-UI-SPEC.md
@.planning/phases/01-visual-foundation-and-public-shell/01-SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve current npm package set</name>
  <read_first>
    - `.planning/phases/01-visual-foundation-and-public-shell/01-RESEARCH.md` section `Package Legitimacy Audit`
    - `.planning/phases/01-visual-foundation-and-public-shell/01-PATTERNS.md` section `Scaffold and Tooling Config`
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>Before any package-manager install, verify each research-flagged package independently and ask the user to approve the pinned set: `create-next-app@16.2.9`, `next@16.2.9`, `react@19.2.7`, `react-dom@19.2.7`, `tailwindcss@4.3.2`, `@tailwindcss/postcss@4.3.2`, `postcss@8.5.16`, `motion@12.42.0`, `lucide-react@1.22.0`, `@playwright/test@1.61.1`, `eslint@10.6.0`, and `eslint-config-next@16.2.9`. Record the verification output and approval signal in the summary. This implements the package supply-chain mitigation for T-01-SC.</action>
  <verify>
    <automated>for pkg in 'create-next-app@16.2.9' 'next@16.2.9' 'react@19.2.7' 'react-dom@19.2.7' 'tailwindcss@4.3.2' '@tailwindcss/postcss@4.3.2' 'postcss@8.5.16' 'motion@12.42.0' 'lucide-react@1.22.0' '@playwright/test@1.61.1' 'eslint@10.6.0' 'eslint-config-next@16.2.9'; do npm view "$pkg" name version repository.url scripts.postinstall || exit 1; done</automated>
    <human-check>Confirm the package names, versions, source repositories, and postinstall output are acceptable before installation.</human-check>
  </verify>
  <acceptance_criteria>
    - User explicitly approves the exact current npm package set.
    - The automated command checks every `SUS` package separately.
    - No package-manager install command has run before approval.
  </acceptance_criteria>
  <done>The executor has a recorded approval signal and downstream install plans may use the pinned package set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local repo | Third-party packages are selected for later scaffold/install work. |
| Research audit -> human approval | Automated package metadata is reviewed by the user before trust is granted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm package selection | high | mitigate | Verify every `SUS` package with separate exact-version `npm view` calls and block installation until human approval is recorded. |
| T-01-01 | Repudiation | Package approval evidence | medium | mitigate | Record the checked package names, versions, repository URLs, postinstall output, and approval signal in the plan summary. |
</threat_model>

<verification>
Overall plan checks:
- `for pkg in 'create-next-app@16.2.9' 'next@16.2.9' 'react@19.2.7' 'react-dom@19.2.7' 'tailwindcss@4.3.2' '@tailwindcss/postcss@4.3.2' 'postcss@8.5.16' 'motion@12.42.0' 'lucide-react@1.22.0' '@playwright/test@1.61.1' 'eslint@10.6.0' 'eslint-config-next@16.2.9'; do npm view "$pkg" name version repository.url scripts.postinstall || exit 1; done`
- Human approval is recorded before any package install appears in shell history or generated files.
</verification>

<success_criteria>
Plan 01-01 is complete when all flagged packages have individual metadata checks and the user has approved the exact pinned package set for Phase 1 implementation.
</success_criteria>

<output>
Create `.planning/phases/01-visual-foundation-and-public-shell/01-01-SUMMARY.md` when done.
</output>
