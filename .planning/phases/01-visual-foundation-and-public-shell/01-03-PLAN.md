---
phase: 01-visual-foundation-and-public-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/app/__skeleton/page.tsx
  - src/components/skeleton/SkeletonProbeClient.tsx
  - src/app/api/skeleton-probe/route.ts
  - src/lib/skeleton/probe-store.ts
  - src/tests/e2e/skeleton.spec.ts
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - "A local skeleton probe demonstrates browser UI to API to isolated runtime data read/write without introducing content, auth, CMS, or schema files."
    - "The skeleton probe is unlisted and does not appear in the public header, homepage route strip, or footer."
    - "The RED skeleton browser proof from Plan 01-02 turns GREEN."
  artifacts:
    - path: "src/app/__skeleton/page.tsx"
      provides: "Unlisted local skeleton probe page"
      exports: ["default"]
    - path: "src/components/skeleton/SkeletonProbeClient.tsx"
      provides: "Client UI action wired to the skeleton probe API"
      exports: ["SkeletonProbeClient"]
    - path: "src/app/api/skeleton-probe/route.ts"
      provides: "Isolated GET/POST skeleton probe API"
      exports: ["GET", "POST"]
    - path: "src/lib/skeleton/probe-store.ts"
      provides: "Local runtime read/write store for the skeleton probe"
      exports: ["readProbe", "writeProbe"]
  key_links:
    - from: "src/components/skeleton/SkeletonProbeClient.tsx"
      to: "src/app/api/skeleton-probe/route.ts"
      via: "fetch('/api/skeleton-probe') GET and POST calls"
      pattern: "fetch\\('/api/skeleton-probe"
    - from: "src/app/api/skeleton-probe/route.ts"
      to: "src/lib/skeleton/probe-store.ts"
      via: "readProbe/writeProbe imports"
      pattern: "readProbe|writeProbe"
---

<objective>
Implement the isolated Walking Skeleton probe that proves local browser UI to API to read/write wiring.

Purpose: Keep the project end-to-end runnable without pulling database, auth, admin, content, or article models into the visual-shell phase.
Output: `/__skeleton` probe page, `/api/skeleton-probe` API route, local runtime store, passing skeleton browser test, and `.planning/phases/01-visual-foundation-and-public-shell/01-03-SUMMARY.md`.
</objective>

## Phase Goal

**As a** public visitor, **I want to** open a distinctive technical-lab public shell with clear routes into the owner's content areas, **so that** I can understand the programmer identity while the site stays readable and responsive.

## Artifacts This Phase Produces (Plan 01-03 Slice)

- `src/app/__skeleton/page.tsx`: unlisted local probe page.
- `src/components/skeleton/SkeletonProbeClient.tsx`: exported `SkeletonProbeClient`.
- `src/app/api/skeleton-probe/route.ts`: exported `GET` and `POST`.
- `src/lib/skeleton/probe-store.ts`: exported `readProbe` and `writeProbe`.
- `src/tests/e2e/skeleton.spec.ts`: passing browser proof for the isolated skeleton slice.

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-and-public-shell/01-CONTEXT.md
@.planning/phases/01-visual-foundation-and-public-shell/01-RESEARCH.md
@.planning/phases/01-visual-foundation-and-public-shell/01-PATTERNS.md
@.planning/phases/01-visual-foundation-and-public-shell/01-UI-SPEC.md
@.planning/phases/01-visual-foundation-and-public-shell/01-SKELETON.md
@.planning/phases/01-visual-foundation-and-public-shell/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement isolated skeleton probe</name>
  <read_first>
    - `.planning/phases/01-visual-foundation-and-public-shell/01-SKELETON.md`
    - `.planning/phases/01-visual-foundation-and-public-shell/01-RESEARCH.md` sections `Architectural Responsibility Map`, `Security Domain`, and `Common Pitfalls`
    - `.planning/phases/01-visual-foundation-and-public-shell/01-PATTERNS.md` sections `Playwright Verification` and `Shared Patterns`
  </read_first>
  <files>src/app/__skeleton/page.tsx, src/components/skeleton/SkeletonProbeClient.tsx, src/app/api/skeleton-probe/route.ts, src/lib/skeleton/probe-store.ts, src/tests/e2e/skeleton.spec.ts</files>
  <behavior>
    - GET `/api/skeleton-probe` returns a JSON object with `status`, `count`, and `updatedAt`.
    - POST `/api/skeleton-probe` increments `count`, persists the new state to an isolated local runtime file, and returns the updated JSON.
    - `/__skeleton` renders one client control that calls the API and displays the updated read result.
  </behavior>
  <action>Create `src/lib/skeleton/probe-store.ts` with `readProbe` and `writeProbe` using Node `fs/promises` against `.next/cache/skeleton-probe.json`; keep the stored object tiny and limited to `count` plus `updatedAt`. Create `src/app/api/skeleton-probe/route.ts` with `runtime = "nodejs"`, exported `GET` and `POST`, no request body parsing, and no secret/env access. Create `src/components/skeleton/SkeletonProbeClient.tsx` as a client component with one button labelled `Write probe`, one status region, and fetch calls to `/api/skeleton-probe`. Create unlisted route `src/app/__skeleton/page.tsx` that renders the probe page for local skeleton verification only; do not link it from public navigation or public route surfaces. Keep the public landing surface server-rendered and readable before hydration. Document in the summary that no schema push is required because this plan introduces no schema-relevant files.</action>
  <verify>
    <automated>npm run lint && npm run build && npx playwright test src/tests/e2e/skeleton.spec.ts --project=desktop</automated>
  </verify>
  <acceptance_criteria>
    - The skeleton browser proof passes.
    - `/api/skeleton-probe` supports GET and POST without article, admin, auth, CMS, or search behavior.
    - `/__skeleton` is unlisted and isolated from public content navigation.
    - No schema push/migration task is needed because no schema files are introduced.
  </acceptance_criteria>
  <done>The local app has a working skeleton probe isolated from public content and later durable data/auth scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/api/skeleton-probe` | Untrusted browser requests reach the isolated local skeleton API. |
| Server runtime -> local cache file | Skeleton API reads/writes a tiny local runtime proof file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs | high | mitigate | No package-manager install occurs in this plan; dependency approval and install are handled by Plans 01-01 and 01-02. |
| T-01-05 | Information Disclosure | `src/app/api/skeleton-probe/route.ts` | low | mitigate | Do not read env vars, filesystem outside `.next/cache/skeleton-probe.json`, request bodies, cookies, or headers; return only `status`, `count`, and `updatedAt`. |
| T-01-06 | Denial of Service | `src/lib/skeleton/probe-store.ts` | low | mitigate | Store one tiny JSON object, overwrite rather than append, and avoid user-provided content in the file. |
</threat_model>

<verification>
Overall plan checks:
- `npm run lint`
- `npm run build`
- `npx playwright test src/tests/e2e/skeleton.spec.ts --project=desktop`
- `test ! -f prisma/schema.prisma && test ! -d prisma/migrations`
- `node -e "const fs=require('fs'); const p='src/app/api/skeleton-probe/route.ts'; const s=fs.readFileSync(p,'utf8'); if(!s.includes('export async function GET')||!s.includes('export async function POST')) process.exit(1)"`
</verification>

<success_criteria>
Plan 01-03 is complete when the isolated skeleton probe passes its browser proof, stores only local runtime probe state, stays unlisted from public navigation, and introduces no database/auth/content schema work.
</success_criteria>

<output>
Create `.planning/phases/01-visual-foundation-and-public-shell/01-03-SUMMARY.md` when done.
</output>
