---
phase: 01-visual-foundation-and-public-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - eslint.config.mjs
  - playwright.config.ts
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
  - src/tests/e2e/skeleton.spec.ts
autonomous: true
requirements:
  - VIS-03
  - QUAL-02
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - "The repo contains a runnable Next.js App Router scaffold with TypeScript, Tailwind v4, ESLint, build, and Playwright scripts."
    - "Global CSS tokens establish the technical-lab visual foundation before client effects hydrate."
    - "A RED skeleton browser proof exists and fails because the skeleton route/API has not been implemented yet."
    - "No content, auth, CMS, search, Prisma, or migration files are introduced in this scaffold slice."
  artifacts:
    - path: "package.json"
      provides: "Pinned scripts and dependencies for Next.js, React, Tailwind, Motion, lucide-react, ESLint, and Playwright"
      contains: "test:e2e"
    - path: "playwright.config.ts"
      provides: "Local dev-server-backed Playwright config with a desktop project"
      contains: "webServer"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 import, technical-lab color/spacing/typography tokens, static fallback background base"
      contains: "@theme"
    - path: "src/tests/e2e/skeleton.spec.ts"
      provides: "RED browser contract for the isolated walking-skeleton probe"
      contains: "__skeleton"
  key_links:
    - from: ".planning/phases/01-visual-foundation-and-public-shell/01-01-SUMMARY.md"
      to: "package.json"
      via: "Package install uses only the approved exact versions"
      pattern: "16.2.9|19.2.7|4.3.2|12.42.0|1.22.0|1.61.1"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "root layout imports global stylesheet"
      pattern: "import './globals.css'"
---

<objective>
Create the Phase 1 app scaffold, tooling baseline, Tailwind token foundation, and RED skeleton browser contract.

Purpose: Establish a runnable full-stack Next.js foundation after package approval while keeping durable data/auth/content work out of Phase 1.
Output: App scaffold, package scripts, global design tokens, Playwright config, RED skeleton browser test, and `.planning/phases/01-visual-foundation-and-public-shell/01-02-SUMMARY.md`.
</objective>

## Phase Goal

**As a** public visitor, **I want to** open a distinctive technical-lab public shell with clear routes into the owner's content areas, **so that** I can understand the programmer identity while the site stays readable and responsive.

## Artifacts This Phase Produces (Plan 01-02 Slice)

- `package.json`: scripts `dev`, `build`, `lint`, and `test:e2e`; exact dependencies from the approved package set.
- `package-lock.json`: generated npm lockfile for the approved package set.
- `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `eslint.config.mjs`: greenfield Next.js, TypeScript, Tailwind, and ESLint configuration.
- `playwright.config.ts`: local `npm run dev` web server and desktop browser project.
- `src/app/layout.tsx`: root metadata and global stylesheet import.
- `src/app/globals.css`: Tailwind v4 tokens and static technical-lab fallback base.
- `src/app/(public)/layout.tsx` and `src/app/(public)/page.tsx`: neutral server-rendered public route group surface for later shell work.
- `src/tests/e2e/skeleton.spec.ts`: failing browser proof for the isolated skeleton slice.

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-and-public-shell/01-CONTEXT.md
@.planning/phases/01-visual-foundation-and-public-shell/01-RESEARCH.md
@.planning/phases/01-visual-foundation-and-public-shell/01-PATTERNS.md
@.planning/phases/01-visual-foundation-and-public-shell/01-UI-SPEC.md
@.planning/phases/01-visual-foundation-and-public-shell/01-SKELETON.md
@.planning/phases/01-visual-foundation-and-public-shell/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold app and tooling after package approval</name>
  <read_first>
    - `.planning/phases/01-visual-foundation-and-public-shell/01-01-SUMMARY.md`
    - `.planning/phases/01-visual-foundation-and-public-shell/01-RESEARCH.md` sections `Standard Stack`, `Architecture Patterns`, and `Environment Availability`
    - `.planning/phases/01-visual-foundation-and-public-shell/01-PATTERNS.md` sections `Scaffold and Tooling Config` and `App Router Layouts and Pages`
    - `.planning/phases/01-visual-foundation-and-public-shell/01-UI-SPEC.md` sections `Design System`, `Layout Contract`, and `Motion and Effect Contract`
  </read_first>
  <files>package.json, package-lock.json, next.config.ts, tsconfig.json, postcss.config.mjs, eslint.config.mjs, playwright.config.ts, src/app/layout.tsx, src/app/globals.css, src/app/(public)/layout.tsx, src/app/(public)/page.tsx</files>
  <action>Confirm Plan 01-01 recorded approval for the exact package set, then scaffold the app in the repository root with Next.js App Router, TypeScript, Tailwind, ESLint, `src/`, npm, and alias `@/*`. If `create-next-app` refuses the non-empty root because `AGENTS.md` or `.planning/` already exist, generate into a sibling temporary scaffold directory and copy only generated app/config files back into this root while preserving existing planning files. Install only the approved versions of `motion`, `lucide-react`, and `@playwright/test`, then install Playwright Chromium. Replace the generated starter page with a neutral server-rendered lab surface and remove starter marketing/external links. Add scripts `dev`, `build`, `lint`, and `test:e2e` to `package.json`. Define initial Tailwind v4 `@theme` tokens in `src/app/globals.css` for lab base, surface, text, muted text, mint accent, 8px radius, and static grid/fallback background so QUAL-02 has a server-rendered visual baseline.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Next.js App Router scaffold exists with TypeScript, Tailwind v4, ESLint, npm, and `@/*` alias.
    - `package.json` uses only approved pinned dependencies and has `dev`, `build`, `lint`, and `test:e2e` scripts.
    - `src/app/globals.css` contains Tailwind v4 lab tokens and static fallback styling.
    - The generated starter page does not expose starter marketing links, fake project links, or fake article content.
    - No `prisma/schema.prisma`, migration, auth, admin, article, CMS, or search implementation file exists.
  </acceptance_criteria>
  <done>The scaffold builds and lints with the approved package set and a server-rendered technical-lab token baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED skeleton browser proof</name>
  <read_first>
    - `.planning/phases/01-visual-foundation-and-public-shell/01-SKELETON.md`
    - `.planning/phases/01-visual-foundation-and-public-shell/01-PATTERNS.md` section `Playwright Verification`
  </read_first>
  <files>playwright.config.ts, src/tests/e2e/skeleton.spec.ts</files>
  <behavior>
    - Browser opens `/__skeleton` and sees a local skeleton probe heading.
    - Browser activates one probe control and sees a persisted counter or timestamp change after a POST to `/api/skeleton-probe`.
    - Until Plan 01-03 implements the route/API/store, the browser proof fails because those files and routes are missing.
  </behavior>
  <action>Create `src/tests/e2e/skeleton.spec.ts` as the RED end-to-end proof for `/__skeleton` and `/api/skeleton-probe`. Keep the test focused on the Walking Skeleton contract: a page-visible probe heading, a `Write probe` control, a GET read, a POST write, and a visible updated result. Do not create the skeleton route, client component, API route, or store in this plan; those are Plan 01-03 outputs.</action>
  <verify>
    <automated>set -e; npm run lint; npm run build; test ! -f src/app/__skeleton/page.tsx; test ! -f src/app/api/skeleton-probe/route.ts; red_output="$(mktemp)"; set +e; npx playwright test src/tests/e2e/skeleton.spec.ts --project=desktop >"$red_output" 2>&1; status=$?; set -e; test "$status" -ne 0; grep -Eq "(__skeleton|Skeleton Probe|Write probe|/api/skeleton-probe)" "$red_output"; rm -f "$red_output"</automated>
  </verify>
  <acceptance_criteria>
    - The RED Playwright test is present and fails because the skeleton route/API is not implemented yet.
    - Lint and build remain independent checks and must pass before the expected browser failure is accepted.
    - The expected failure is tied to missing skeleton files/routes, not arbitrary Playwright failure.
  </acceptance_criteria>
  <done>The skeleton browser proof is failing for the expected missing route/API behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approved package set -> local repo | Package installs create executable project dependencies. |
| Root scaffold -> planning artifacts | Generated app files enter a repo that already contains GSD planning files. |
| Browser -> dev server | Playwright opens the scaffolded app through the local dev server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs | high | mitigate | This plan depends on Plan 01-01 approval and installs only exact approved versions. |
| T-01-02 | Tampering | Scaffold copy-back | medium | mitigate | Preserve `AGENTS.md` and `.planning/`; copy only generated app/config files if temporary scaffold is needed. |
| T-01-03 | Spoofing | Generated starter page | medium | mitigate | Remove default scaffold marketing/external links and avoid fake social/project destinations. |
| T-01-04 | Information Disclosure | Initial public route group | low | mitigate | Render only static public lab copy; do not read env vars, secrets, diagnostics, admin paths, or runtime data. |
</threat_model>

<verification>
Overall plan checks:
- `npm run lint`
- `npm run build`
- `test ! -f prisma/schema.prisma && test ! -d prisma/migrations`
- `set -e; npm run lint; npm run build; test ! -f src/app/__skeleton/page.tsx; test ! -f src/app/api/skeleton-probe/route.ts; red_output="$(mktemp)"; set +e; npx playwright test src/tests/e2e/skeleton.spec.ts --project=desktop >"$red_output" 2>&1; status=$?; set -e; test "$status" -ne 0; grep -Eq "(__skeleton|Skeleton Probe|Write probe|/api/skeleton-probe)" "$red_output"; rm -f "$red_output"`
</verification>

<success_criteria>
Plan 01-02 is complete when the approved package set is installed, the app scaffold lints/builds, Tailwind lab tokens exist, and the RED skeleton browser proof fails only because the skeleton route/API slice is intentionally absent until Plan 01-03.
</success_criteria>

<output>
Create `.planning/phases/01-visual-foundation-and-public-shell/01-02-SUMMARY.md` when done.
</output>
