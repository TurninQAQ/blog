---
phase: 06-brand-homepage-and-secure-authoring-experience-upgrade-with-
plan: "02"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [EDIT-05, EDIT-07]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/admin/wysiwyg/text-tone.ts
  - src/lib/admin/wysiwyg/extensions.ts
  - src/lib/admin/wysiwyg/compatibility.ts
  - src/lib/markdown/public-render.tsx
  - src/lib/markdown/markdown-policy.ts
  - src/components/admin/wysiwyg/WysiwygToolbar.tsx
  - src/components/admin/PostEditorShell.tsx
  - src/lib/admin/markdown-import.ts
  - src/app/admin/admin.css
---

<objective>Add the requested formatting controls and safe Markdown import without weakening the raw-HTML boundary or replacing the editor.</objective>

<tasks>

1. Add data-driven toolbar controls for selection bold, unordered/ordered lists, body/H1-H4, four safe text tones, and the existing code block behavior.
2. Serialize tones as allowlisted `:tone-*[]` directives; render fixed classes publicly through `remark-directive`; keep arbitrary HTML/styles blocked and test nested bold/tone round trips.
3. Add a bounded new-draft `.md/.markdown` importer using known frontmatter fields, title/H1/filename fallback, atomic compatibility validation, and draft-only semantics.
4. Cover toolbar keyboard state, serialization, hostile directive/raw HTML cases, and import fallbacks/errors with unit and browser tests.

</tasks>

<verification>`npm run test:unit`, targeted admin authoring/public rendering Playwright, `npm run lint`, and `npm run build`.</verification>
