---
phase: 04-public-content-library
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260706000000_add_post_featured/migration.sql
  - src/generated/prisma/client.ts
  - src/generated/prisma/browser.ts
  - src/generated/prisma/models.ts
  - src/generated/prisma/models/Post.ts
  - src/generated/prisma/commonInputTypes.ts
  - src/generated/prisma/internal/prismaNamespace.ts
  - src/tests/e2e/data-model-foundation.spec.ts
autonomous: true
requirements:
  - CMS-05
  - CMS-06
must_haves:
  truths:
    - "D-17: `Post` has a real `featured Boolean @default(false)` field."
    - "D-17: generated Prisma types expose the `featured` field before admin controls consume it."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Post.featured Boolean @default(false)"
    - path: "prisma/migrations/20260706000000_add_post_featured/migration.sql"
      provides: "database migration for the featured column"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/generated/prisma/models/Post.ts"
      via: "npm run db:generate"
      pattern: "generated featured field"
---

## Phase Goal

**As a** site owner, **I want to** explicitly mark featured articles in stored data, **so that** homepage featured content is a real author choice.

<objective>
Add the `Post.featured` schema field and run the required database push/generate gate.

Purpose: D-17 requires a real featured field before admin controls and homepage featured modules can be implemented.
Output: Prisma schema field, migration, generated client updates, and data-model regression coverage.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-public-content-library/04-CONTEXT.md
@.planning/phases/04-public-content-library/04-RESEARCH.md
@.planning/phases/04-public-content-library/04-PATTERNS.md
@prisma/schema.prisma
@src/tests/e2e/data-model-foundation.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Add featured schema, migration, database push, and generated Prisma client</name>
  <files>prisma/schema.prisma, prisma/migrations/20260706000000_add_post_featured/migration.sql, src/generated/prisma/client.ts, src/generated/prisma/browser.ts, src/generated/prisma/models.ts, src/generated/prisma/models/Post.ts, src/generated/prisma/commonInputTypes.ts, src/generated/prisma/internal/prismaNamespace.ts, src/tests/e2e/data-model-foundation.spec.ts</files>
  <read_first>
    - `.planning/phases/04-public-content-library/04-CONTEXT.md`
    - `.planning/phases/04-public-content-library/04-RESEARCH.md`
    - `.planning/phases/04-public-content-library/04-PATTERNS.md`
    - `.planning/REQUIREMENTS.md`
    - `prisma/schema.prisma`
    - `prisma/migrations/20260703020348_init_content_admin/migration.sql`
    - `src/tests/e2e/data-model-foundation.spec.ts`
  </read_first>
  <action>Add `featured Boolean @default(false)` to `model Post` near publication fields, and create `prisma/migrations/20260706000000_add_post_featured/migration.sql` that adds a non-null boolean column with default false. Update `data-model-foundation.spec.ts` to assert schema and generated type support for the field. After schema and migration edits, run `node --env-file-if-exists=.env.local ./node_modules/.bin/prisma db push`; if Prisma reports destructive changes or asks for data-loss confirmation, STOP, record the exact prompt/blocker in the plan summary/deviation, and require human approval or a revised non-destructive migration before continuing. Do not continue autonomously with any data-loss flag. If the schema push succeeds without a destructive prompt, run `npm run db:generate` and do not manually edit generated Prisma files.</action>
  <acceptance_criteria>
    - `prisma/schema.prisma` defines `featured Boolean @default(false)` on `Post`.
    - The migration SQL adds a non-null `featured` column with default false and does not drop or rewrite existing content columns.
    - `node --env-file-if-exists=.env.local ./node_modules/.bin/prisma db push` succeeds without destructive confirmation, or execution stops with the exact Prisma prompt recorded for human approval/revision.
    - Generated Prisma files expose the `featured` field only through `npm run db:generate`.
    - `data-model-foundation.spec.ts` asserts schema and generated-client support for `featured`.
  </acceptance_criteria>
  <verify>
    <automated>node --env-file-if-exists=.env.local ./node_modules/.bin/prisma db push && npm run db:generate && npm run lint && npm run build && npx playwright test src/tests/e2e/data-model-foundation.spec.ts --project=desktop</automated>
  </verify>
  <done>The blocking Prisma schema push/migration task for `featured` succeeds, and generated Prisma files expose the field for later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma schema -> database | Schema changes alter durable content rows. |
| generated client -> application code | Generated types become trusted compile-time and runtime interfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06-01 | Tampering | Prisma schema push | high | mitigate | Create explicit migration and run `node --env-file-if-exists=.env.local ./node_modules/.bin/prisma db push`; if Prisma reports destructive changes or requests data-loss confirmation, stop, record the exact blocker in the summary/deviation, and require human approval or a revised non-destructive migration before generating the client. |
| T-04-06-02 | Information Disclosure | featured flag default | low | mitigate | Default existing rows to false so no current post is accidentally treated as featured. |
| T-04-SC | Tampering | npm installs | high | mitigate | No package installation is authorized in this plan. |
</threat_model>

<verification>
Overall plan verification:
- `node --env-file-if-exists=.env.local ./node_modules/.bin/prisma db push`
- `npm run db:generate`
- `npm run lint`
- `npm run build`
- `npx playwright test src/tests/e2e/data-model-foundation.spec.ts --project=desktop`
</verification>

<success_criteria>
- D-17 has durable schema support.
- The required `[BLOCKING]` Prisma schema push/migration gate is complete.
- Plan 04-07 can implement admin controls against generated `featured` types.
</success_criteria>

<output>
Create `.planning/phases/04-public-content-library/04-06-SUMMARY.md` when done.
</output>
