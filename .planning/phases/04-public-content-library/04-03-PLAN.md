---
phase: 04-public-content-library
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/tests/e2e/public-content-library.spec.ts
  - src/tests/e2e/public-shell.spec.ts
  - src/app/(public)/notes/page.tsx
  - src/components/public/content/PublicEmptyState.tsx
  - src/components/public/content/PublicNoteList.tsx
  - src/components/public/content/PublicNoteCard.tsx
  - src/components/public/content/PostVisualBlock.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - READ-01
must_haves:
  truths:
    - "D-05: `/notes` renders a dense published-only article list with title, excerpt, tags, category, publish date, and reading time."
    - "D-08: posts without covers render a technical-lab CSS visual block."
    - "D-19: empty `/notes` state uses polished Chinese copy and exposes no admin links or instructions."
    - "D-20 and D-23: tests use temporary `phase-4-` fixtures and assert drafts do not appear in the list."
  artifacts:
    - path: "src/app/(public)/notes/page.tsx"
      provides: "published public article list route"
    - path: "src/components/public/content/PublicNoteList.tsx"
      provides: "dense public notes list"
    - path: "src/components/public/content/PostVisualBlock.tsx"
      provides: "cover-or-CSS technical lab visual treatment"
  key_links:
    - from: "src/app/(public)/notes/page.tsx"
      to: "src/lib/public/content-queries.ts"
      via: "getPublishedPostList()"
      pattern: "published-only list helper"
---

## Phase Goal

**As a** public reader, **I want to** browse published technical notes, **so that** I can scan real public articles without seeing drafts.

<objective>
Build the public notes list route on top of the Plan 04-02 foundation.

Purpose: This replaces the inactive `/notes` placeholder with real published content while keeping draft non-disclosure observable.
Output: `/notes`, dense list components, fallback visual blocks, empty-state copy, CSS, and browser coverage.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-public-content-library/04-CONTEXT.md
@.planning/phases/04-public-content-library/04-UI-SPEC.md
@.planning/phases/04-public-content-library/04-PATTERNS.md
@.planning/phases/04-public-content-library/04-02-SUMMARY.md
@src/lib/public/content-queries.ts
@src/app/(public)/notes/page.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED public notes list browser coverage</name>
  <files>src/tests/e2e/public-content-library.spec.ts, src/tests/e2e/public-shell.spec.ts</files>
  <read_first>
    - `.planning/phases/04-public-content-library/04-CONTEXT.md`
    - `.planning/phases/04-public-content-library/04-UI-SPEC.md`
    - `.planning/phases/04-public-content-library/04-PATTERNS.md`
    - `.planning/phases/04-public-content-library/04-02-SUMMARY.md`
    - `src/tests/e2e/public-content-library.spec.ts`
    - `src/tests/e2e/public-shell.spec.ts`
    - `src/lib/public/content-queries.ts`
  </read_first>
  <behavior>
    - D-05 and READ-01: a published fixture appears on `/notes` with title, excerpt, category, tags, publish date, reading time, and visual treatment.
    - D-08: a published post without a cover image renders the CSS technical-lab visual block.
    - D-19 and D-20: empty database state shows polished Chinese copy and no committed public article data.
    - D-23: a draft fixture sharing relevant metadata does not appear in `/notes`.
  </behavior>
  <action>Extend `public-content-library.spec.ts` with route-level tests for `/notes`, draft absence, cover/fallback visual behavior, metadata display, stable responsive dimensions, and Chinese empty state copy. Reuse the Plan 04-02 direct Prisma `phase-4-` fixture helpers and cleanup. Update `public-shell.spec.ts` only where the previous inactive `/notes` placeholder copy conflicts with real published-route behavior.</action>
  <acceptance_criteria>
    - `public-content-library.spec.ts` has failing-before-implementation assertions for `/notes`, draft absence, metadata display, visual treatment, responsive dimensions, and empty state copy.
    - The tests create only temporary `phase-4-` fixture data and clean it up.
    - `public-shell.spec.ts` no longer expects inactive placeholder copy for `/notes`.
    - The desktop Playwright command runs against the new RED coverage.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run build && npx playwright test src/tests/e2e/public-content-library.spec.ts --project=desktop</automated>
  </verify>
  <done>RED browser coverage exists for notes list behavior and draft non-disclosure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement notes list route and dense list components</name>
  <files>src/app/(public)/notes/page.tsx, src/components/public/content/PublicEmptyState.tsx, src/components/public/content/PublicNoteList.tsx, src/components/public/content/PublicNoteCard.tsx, src/components/public/content/PostVisualBlock.tsx, src/app/globals.css</files>
  <read_first>
    - `.planning/phases/04-public-content-library/04-CONTEXT.md`
    - `.planning/phases/04-public-content-library/04-UI-SPEC.md`
    - `.planning/phases/04-public-content-library/04-PATTERNS.md`
    - `.planning/phases/04-public-content-library/04-02-SUMMARY.md`
    - `src/lib/public/content-queries.ts`
    - `src/app/(public)/notes/page.tsx`
    - `src/components/admin/AdminPostList.tsx`
    - `src/components/public/FeaturedNoteCard.tsx`
    - `src/components/public/ArticlePreviewShell.tsx`
  </read_first>
  <behavior>
    - D-05 and READ-01: `/notes` uses `getPublishedPostList()` and dense scan-friendly rows.
    - D-08: `PostVisualBlock` provides stable cover/fallback dimensions.
    - D-19: empty `/notes` state uses Chinese copy and no admin links.
  </behavior>
  <action>Replace `/notes` as a server component using `getPublishedPostList()`. Add `PublicEmptyState`, `PublicNoteList`, `PublicNoteCard`, and `PostVisualBlock` with UI-SPEC spacing, 8px radius, 44px link targets, cover-or-CSS visual treatment, wrapped metadata, and Chinese visible copy. Do not add route-local Prisma queries.</action>
  <acceptance_criteria>
    - `/notes` imports and calls the Plan 04-02 public list helper instead of Prisma directly.
    - Published rows show title, excerpt, category, tags, publish date, reading time, and a cover or CSS technical-lab visual block.
    - Empty state text is Chinese-first and contains no `/admin` link or publishing instructions.
    - List rows and visual blocks have stable responsive dimensions and no page-level horizontal overflow in the Playwright projects.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run build && npx playwright test src/tests/e2e/public-content-library.spec.ts src/tests/e2e/public-shell.spec.ts --project=desktop --project=mobile --project=min-mobile --project=reduced-motion && ! rg "dangerouslySetInnerHTML|rehype-raw" src/lib/markdown src/components/public src/app/'(public)'</automated>
  </verify>
  <done>Public notes list is complete with metadata, fallback visuals, empty states, and published-only behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database -> `/notes` route | Stored post summaries cross into the public article list. |
| stored cover URL -> visual component | Optional cover data becomes public visual output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | `/notes` | high | mitigate | Route consumes `getPublishedPostList()` only and tests include a matching draft fixture. |
| T-04-03-02 | Information Disclosure | `PublicEmptyState` | low | mitigate | Empty state copy must not link to `/admin` or mention publishing workflow instructions. |
| T-04-SC | Tampering | npm installs | high | mitigate | No package installation is authorized in this plan; dependencies were installed in 04-02 after 04-01 approval. |
</threat_model>

<verification>
Overall plan verification:
- `npm run lint`
- `npm run build`
- `npx playwright test src/tests/e2e/public-content-library.spec.ts src/tests/e2e/public-shell.spec.ts --project=desktop --project=mobile --project=min-mobile --project=reduced-motion`
- `! rg "dangerouslySetInnerHTML|rehype-raw" src/lib/markdown src/components/public src/app/'(public)'`
</verification>

<success_criteria>
- READ-01 is observable on `/notes`.
- D-05, D-08, D-19, D-20, and D-23 have browser or source-scan coverage.
- Plan 04-09 can build `/notes/[slug]` without also owning the public list route.
</success_criteria>

<output>
Create `.planning/phases/04-public-content-library/04-03-SUMMARY.md` when done.
</output>
