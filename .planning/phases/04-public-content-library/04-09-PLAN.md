---
phase: 04-public-content-library
plan: "09"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - src/tests/e2e/public-content-library.spec.ts
  - src/app/(public)/notes/[slug]/page.tsx
  - src/components/public/content/ArticlePage.tsx
  - src/components/public/content/ArticleHeader.tsx
  - src/components/public/content/ArticleMarkdown.tsx
  - src/components/public/content/TableOfContents.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - READ-02
  - READ-03
  - READ-04
  - READ-05
  - READ-06
  - READ-07
must_haves:
  truths:
    - "D-02: draft, archived, unpublished, and nonexistent detail slugs return the same 404 behavior."
    - "D-06 and D-22: article TOC is desktop rail content and mobile document-flow content."
    - "D-07: code blocks are highlighted server-side with Shiki and scroll inside the code block."
    - "D-21: article reading surfaces stay calm without continuous animation."
  artifacts:
    - path: "src/app/(public)/notes/[slug]/page.tsx"
      provides: "published article detail route and dynamic metadata"
    - path: "src/components/public/content/ArticlePage.tsx"
      provides: "article shell for header, TOC, Markdown body, and later rails"
    - path: "src/components/public/content/TableOfContents.tsx"
      provides: "desktop sticky and mobile in-flow TOC"
  key_links:
    - from: "src/app/(public)/notes/[slug]/page.tsx"
      to: "src/lib/public/content-queries.ts"
      via: "getPublishedPostBySlug(slug)"
      pattern: "notFound on null"
---

## Phase Goal

**As a** public reader, **I want to** open and read published technical notes, **so that** article pages provide safe Markdown, highlighted code, TOC navigation, reading time, and metadata.

<objective>
Build the public article detail route after the public list route exists.

Purpose: This keeps the former 04-03 article-detail scope under the file-count threshold while preserving READ-02 through READ-07.
Output: `/notes/[slug]`, article shell components, safe Markdown integration, TOC layout, metadata, CSS, and browser coverage.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-public-content-library/04-CONTEXT.md
@.planning/phases/04-public-content-library/04-UI-SPEC.md
@.planning/phases/04-public-content-library/04-PATTERNS.md
@.planning/phases/04-public-content-library/04-02-SUMMARY.md
@.planning/phases/04-public-content-library/04-03-SUMMARY.md
@src/lib/public/content-queries.ts
@src/lib/markdown/public-render.tsx
@src/lib/seo/article-metadata.ts
@src/app/(public)/notes/[slug]/page.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED public article detail browser coverage</name>
  <files>src/tests/e2e/public-content-library.spec.ts</files>
  <read_first>
    - `.planning/phases/04-public-content-library/04-CONTEXT.md`
    - `.planning/phases/04-public-content-library/04-UI-SPEC.md`
    - `.planning/phases/04-public-content-library/04-PATTERNS.md`
    - `.planning/phases/04-public-content-library/04-02-SUMMARY.md`
    - `.planning/phases/04-public-content-library/04-03-SUMMARY.md`
    - `src/tests/e2e/public-content-library.spec.ts`
    - `src/lib/public/content-queries.ts`
    - `src/lib/markdown/public-render.tsx`
  </read_first>
  <behavior>
    - D-02 and D-23: a draft fixture with a known slug returns 404 and is indistinguishable from a nonexistent slug.
    - D-06 and READ-05: article detail shows desktop TOC rail and mobile in-flow TOC before the article body.
    - D-07 and READ-04: fenced code renders highlighted code and scrolls internally without page overflow.
    - READ-03: headings, links, lists, tables, and images render in the article body.
    - READ-06: article detail exposes estimated reading time.
    - READ-07: article detail metadata contains title and excerpt-derived metadata.
  </behavior>
  <action>Extend `public-content-library.spec.ts` with route-level tests for `/notes/[slug]`, draft detail 404, Markdown rendering, code block overflow, desktop/mobile TOC behavior, reading time, article metadata, and calm reading-surface checks. Reuse the Plan 04-02 direct Prisma `phase-4-` fixture helpers and cleanup.</action>
  <acceptance_criteria>
    - Tests assert published article detail renders title, excerpt, category, tags, publish date, reading time, safe Markdown, highlighted code, generated TOC links, and metadata.
    - Tests assert draft, archived, unpublished, and nonexistent slugs all use the same 404 behavior.
    - Tests assert desktop TOC is in the right rail and mobile TOC appears before article body.
    - Tests assert code and table overflow remains internal to those blocks.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run build && npx playwright test src/tests/e2e/public-content-library.spec.ts --project=desktop</automated>
  </verify>
  <done>RED browser coverage exists for article detail behavior and draft non-disclosure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement article detail route, Markdown body, and TOC layout</name>
  <files>src/app/(public)/notes/[slug]/page.tsx, src/components/public/content/ArticlePage.tsx, src/components/public/content/ArticleHeader.tsx, src/components/public/content/ArticleMarkdown.tsx, src/components/public/content/TableOfContents.tsx, src/app/globals.css</files>
  <read_first>
    - `.planning/phases/04-public-content-library/04-CONTEXT.md`
    - `.planning/phases/04-public-content-library/04-UI-SPEC.md`
    - `.planning/phases/04-public-content-library/04-PATTERNS.md`
    - `.planning/phases/04-public-content-library/04-02-SUMMARY.md`
    - `.planning/phases/04-public-content-library/04-03-SUMMARY.md`
    - `src/lib/public/content-queries.ts`
    - `src/lib/markdown/public-render.tsx`
    - `src/lib/seo/article-metadata.ts`
    - `src/app/(public)/notes/[slug]/page.tsx`
    - `src/app/globals.css`
  </read_first>
  <behavior>
    - D-02 and READ-02: `/notes/[slug]` uses `getPublishedPostBySlug(slug)` and calls `notFound()` for null.
    - READ-03 through READ-06: article detail renders safe Markdown, highlighted code, TOC links, and reading time.
    - READ-07: article detail exports metadata from `articleMetadata()`.
    - D-06, D-21, and D-22: TOC uses desktop rail/mobile flow and article prose has no continuous animation.
  </behavior>
  <action>Add `/notes/[slug]` as a server component using the Plan 04-02 public helpers and metadata helper. Add `ArticlePage`, `ArticleHeader`, `ArticleMarkdown`, and `TableOfContents` with semantic article/nav structure, desktop right rail, mobile in-flow TOC before the body, internal code/table overflow, and calm reading styles in `globals.css`. Do not add route-local Prisma queries.</action>
  <acceptance_criteria>
    - `/notes/[slug]` returns the same 404 behavior for draft, archived, unpublished, and nonexistent slugs.
    - Article detail renders title, excerpt, category, tags, publish date, reading time, safe Markdown, highlighted code, and generated TOC links.
    - Desktop layout places TOC in the right rail; mobile places TOC before article body without squeezing prose.
    - Metadata generation uses title, excerpt, cover, and tags from the published post DTO.
    - The unsafe-render source scan passes for public Markdown and article components.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run build && npx playwright test src/tests/e2e/public-content-library.spec.ts src/tests/e2e/public-shell.spec.ts --project=desktop --project=mobile --project=min-mobile --project=reduced-motion && ! rg "dangerouslySetInnerHTML|rehype-raw" src/lib/markdown src/components/public src/app/'(public)'</automated>
  </verify>
  <done>Public article detail is complete with safe Markdown, highlighted code, TOC, reading time, metadata, and draft non-disclosure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `/notes/[slug]` | Untrusted slug selects public article content. |
| database -> article route | Stored post content crosses into server-rendered pages. |
| Markdown renderer -> article component | Rendered Markdown output enters the public article shell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09-01 | Information Disclosure | `/notes/[slug]` | high | mitigate | Call `notFound()` when the published-only helper returns null; tests assert draft and nonexistent slugs behave alike. |
| T-04-09-02 | Tampering | article Markdown output | high | mitigate | Render through Plan 04-02 safe renderer and keep unsafe raw-render source scan in verification. |
| T-04-SC | Tampering | npm installs | high | mitigate | No package installation is authorized in this plan; dependencies were installed in 04-02 after 04-01 approval. |
</threat_model>

<verification>
Overall plan verification:
- `npm run lint`
- `npm run build`
- `npx playwright test src/tests/e2e/public-content-library.spec.ts src/tests/e2e/public-shell.spec.ts --project=desktop --project=mobile --project=min-mobile --project=reduced-motion`
- `! rg "dangerouslySetInnerHTML|rehype-raw" src/lib/markdown src/components/public src/app/'(public)'`
</verification>

<success_criteria>
- READ-02 through READ-07 are observable on public article detail routes.
- D-02, D-06, D-07, D-21, D-22, and D-23 have browser or source-scan coverage.
- Plan 04-04 can add organization coverage/components without also owning article detail implementation.
</success_criteria>

<output>
Create `.planning/phases/04-public-content-library/04-09-SUMMARY.md` when done.
</output>
