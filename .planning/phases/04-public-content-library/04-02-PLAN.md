---
phase: 04-public-content-library
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - package.json
  - package-lock.json
  - src/tests/e2e/public-content-library.spec.ts
  - src/lib/public/content-queries.ts
  - src/lib/markdown/public-render.tsx
  - src/lib/seo/article-metadata.ts
autonomous: true
requirements:
  - READ-02
  - READ-03
  - READ-04
  - READ-05
  - READ-06
  - READ-07
must_haves:
  truths:
    - "D-07: exact Shiki, reading-time, and HAST-to-JSX dependencies are installed only after 04-01 approval."
    - "D-20: Phase 4 public tests use isolated `phase-4-` fixture slugs and taxonomy names with cleanup, not committed sample content."
    - "D-24: public query helpers read live database state through a central published-only boundary."
  artifacts:
    - path: "src/lib/public/content-queries.ts"
      provides: "published-only public DTOs and list/detail helpers"
    - path: "src/lib/markdown/public-render.tsx"
      provides: "safe server Markdown rendering with Shiki HAST output and TOC extraction"
    - path: "src/lib/seo/article-metadata.ts"
      provides: "article metadata helper for detail routes"
  key_links:
    - from: "src/app/(public) routes"
      to: "src/lib/public/content-queries.ts"
      via: "published-only helper imports"
      pattern: "no direct public route Prisma post queries"
---

## Phase Goal

**As a** public reader, **I want to** read published articles rendered from safe Markdown and live published data, **so that** the public content library can be built without draft leakage or unsafe article HTML.

<objective>
Create the Phase 4 dependency, Markdown, SEO, fixture, and public-query foundation.

Purpose: Later public routes should consume one safe rendering/query boundary instead of duplicating filters or Markdown policy.
Output: Exact dependency pins, `content-queries.ts`, `public-render.tsx`, `article-metadata.ts`, and isolated Phase 4 fixture helpers.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-public-content-library/04-CONTEXT.md
@.planning/phases/04-public-content-library/04-RESEARCH.md
@.planning/phases/04-public-content-library/04-PATTERNS.md
@.planning/phases/04-public-content-library/04-UI-SPEC.md
@.planning/phases/04-public-content-library/04-01-SUMMARY.md
@src/components/markdown/MarkdownPreview.tsx
@src/lib/markdown/markdown-policy.ts
@src/lib/admin/post-queries.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install exact public rendering dependencies after Shiki approval</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - `.planning/phases/04-public-content-library/04-01-SUMMARY.md`
    - `.planning/phases/04-public-content-library/04-RESEARCH.md`
    - `.planning/phases/04-public-content-library/04-CONTEXT.md`
    - `package.json`
    - `package-lock.json`
  </read_first>
  <action>Confirm 04-01 approval exists, then run `npm install --save-exact shiki@4.3.0 reading-time@1.5.0 hast-util-to-jsx-runtime@2.3.6`. Verify `package.json` records exact direct pins with no caret ranges using a shell-safe single-quoted `node -e` command that avoids JavaScript template literals inside a double-quoted shell string, and do not introduce Fuse, a dedicated search package, MDX, or raw-HTML Markdown packages, preserving D-07 and D-13.</action>
  <acceptance_criteria>
    - `.planning/phases/04-public-content-library/04-01-SUMMARY.md` contains human approval for `shiki@4.3.0` before install.
    - `package.json` direct dependencies equal `shiki: 4.3.0`, `reading-time: 1.5.0`, and `hast-util-to-jsx-runtime: 2.3.6`.
    - `package-lock.json` resolves the same three packages at those exact versions.
    - No Fuse, dedicated search engine, MDX, or raw-HTML Markdown dependency is added.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const pkg=require("./package.json"); const expected={shiki:"4.3.0","reading-time":"1.5.0","hast-util-to-jsx-runtime":"2.3.6"}; for (const name of Object.keys(expected)) { const version=expected[name]; if ((pkg.dependencies || {})[name] !== version) throw new Error(name + " not pinned to " + version); }'</automated>
  </verify>
  <done>Exact approved runtime dependencies are installed and pinned for READ-04 and READ-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create published query, safe Markdown, metadata, and fixture foundation</name>
  <files>src/tests/e2e/public-content-library.spec.ts, src/lib/public/content-queries.ts, src/lib/markdown/public-render.tsx, src/lib/seo/article-metadata.ts</files>
  <read_first>
    - `.planning/phases/04-public-content-library/04-CONTEXT.md`
    - `.planning/phases/04-public-content-library/04-RESEARCH.md`
    - `.planning/phases/04-public-content-library/04-PATTERNS.md`
    - `.planning/phases/04-public-content-library/04-UI-SPEC.md`
    - `src/components/markdown/MarkdownPreview.tsx`
    - `src/lib/markdown/markdown-policy.ts`
    - `src/lib/admin/post-queries.ts`
    - `src/tests/e2e/admin-authoring.spec.ts`
    - `src/tests/e2e/public-content-library.spec.ts`
  </read_first>
  <behavior>
    - D-02: `getPublishedPostBySlug(slug)` returns only published posts with non-null `publishedAt`.
    - D-07 and READ-04: fenced code is highlighted with Shiki on the server through HAST-to-JSX rendering.
    - READ-03 and EDIT-04: raw Markdown HTML stays disabled and sanitized policy remains aligned with the existing preview boundary.
    - READ-05: Markdown rendering returns heading data for a generated TOC.
    - READ-06: public summary/detail DTOs expose reading time.
    - READ-07: article metadata derives from title, excerpt, cover, and tags.
    - D-20 and D-23: fixture helpers use direct Prisma fixtures for public-surface leakage checks with `phase-4-` slugs/taxonomy names and cleanup.
  </behavior>
  <action>Create `src/lib/public/content-queries.ts` with `PublicPostSummary`, `PublicPostDetail`, `getPublishedPostList()`, and `getPublishedPostBySlug(slug)`. Every helper must filter `PublicationStatus.PUBLISHED` and non-null `publishedAt`, map category/tags/series DTOs, compute reading time, and avoid direct public route data coupling. Create `src/lib/markdown/public-render.tsx` from the existing safe Markdown policy, using GFM, sanitizer, heading id collection, Shiki `codeToHast`, and JSX runtime rendering. Create `src/lib/seo/article-metadata.ts`. Add reusable Phase 4 test fixture helpers inside `public-content-library.spec.ts`; public-surface leakage checks should use direct Prisma fixtures, while admin mutation behavior remains under browser/API tests in Plan 04-07.</action>
  <acceptance_criteria>
    - `content-queries.ts` exports published-only list/detail helpers and no helper returns posts without `status: PUBLISHED` and non-null `publishedAt`.
    - `public-render.tsx` renders Markdown with GFM, sanitizer, heading id collection, server-side Shiki HAST output, and no broad raw HTML injection path.
    - `article-metadata.ts` derives title, description, cover, and tag metadata from a public post DTO.
    - `public-content-library.spec.ts` defines `phase-4-` fixture helpers with cleanup for posts, tags, categories, and series.
    - Lint, build, and the unsafe-render source scan pass.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run build && ! rg "dangerouslySetInnerHTML|rehype-raw" src/lib/markdown src/lib/public src/components/markdown</automated>
  </verify>
  <done>The shared public query, safe Markdown, metadata, and fixture foundation exists for downstream public routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database -> public query helpers | Stored posts and taxonomy data become public DTOs. |
| author Markdown -> React output | Stored Markdown becomes public article UI. |
| npm registry -> runtime dependencies | Newly installed rendering packages enter the trusted runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | `src/lib/public/content-queries.ts` | high | mitigate | Centralize `PUBLISHED` plus non-null `publishedAt` filters and make public routes consume helpers only. |
| T-04-02-02 | Tampering / Elevation of Privilege | `src/lib/markdown/public-render.tsx` | high | mitigate | Keep raw HTML disabled, sanitize rendered output, and use Shiki HAST-to-JSX instead of raw HTML injection. |
| T-04-02-03 | Denial of Service | server code highlighting | medium | mitigate | Perform highlighting on server-rendered article paths, not admin preview or client bundles. |
| T-04-SC | Tampering | npm installs | high | mitigate | Install only after 04-01 approval and verify exact pins for the three allowed packages. |
</threat_model>

<verification>
Overall plan verification:
- Exact dependency pin script
- `npm run lint`
- `npm run build`
- `! rg "dangerouslySetInnerHTML|rehype-raw" src/lib/markdown src/lib/public src/components/markdown`
</verification>

<success_criteria>
- Plan 04-03 can build `/notes` and `/notes/[slug]` without creating new query or Markdown boundaries.
- READ-03 through READ-07 have shared implementation support.
- Fixture strategy follows the resolved research decision for D-20 and D-23.
</success_criteria>

<output>
Create `.planning/phases/04-public-content-library/04-02-SUMMARY.md` when done.
</output>
