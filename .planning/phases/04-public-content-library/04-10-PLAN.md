---
phase: 04-public-content-library
plan: "10"
type: execute
wave: 6
depends_on:
  - "04-04"
files_modified:
  - src/lib/public/content-queries.ts
  - src/app/(public)/tags/[slug]/page.tsx
  - src/app/(public)/categories/[slug]/page.tsx
  - src/app/(public)/archive/page.tsx
  - src/app/(public)/series/page.tsx
  - src/app/(public)/series/[slug]/page.tsx
autonomous: true
requirements:
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04
  - TAX-05
must_haves:
  truths:
    - "D-09: tags and categories use independent semantic routes."
    - "D-10: `/archive` groups published posts by year and month."
    - "D-11: `/series` shows series names and descriptions only."
    - "D-12: series detail orders published posts by `seriesOrder` ascending."
    - "D-23 and TAX-05: drafts never appear on taxonomy, archive, or series surfaces."
  artifacts:
    - path: "src/app/(public)/tags/[slug]/page.tsx"
      provides: "published-only tag route"
    - path: "src/app/(public)/categories/[slug]/page.tsx"
      provides: "published-only category route"
    - path: "src/app/(public)/archive/page.tsx"
      provides: "published archive route"
    - path: "src/app/(public)/series/[slug]/page.tsx"
      provides: "ordered published series route"
  key_links:
    - from: "public organization routes"
      to: "src/lib/public/content-queries.ts"
      via: "published taxonomy/archive/series helpers"
      pattern: "notFound for private-only slugs"
---

## Phase Goal

**As a** public reader, **I want to** browse published notes by tag, category, archive, and series, **so that** I can follow topics and chronology without private drafts appearing.

<objective>
Wire taxonomy, archive, series index, and series detail routes to the organization components from Plan 04-04.

Purpose: This completes the former 04-04 route/query scope while keeping the plan below the file-count threshold.
Output: Published-only organization helpers and live organization routes.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-public-content-library/04-CONTEXT.md
@.planning/phases/04-public-content-library/04-UI-SPEC.md
@.planning/phases/04-public-content-library/04-PATTERNS.md
@.planning/phases/04-public-content-library/04-04-SUMMARY.md
@src/lib/public/content-queries.ts
@src/app/(public)/archive/page.tsx
@src/app/(public)/series/page.tsx
@src/components/public/content/TaxonomyPageHeader.tsx
@src/components/public/content/ArchiveTimeline.tsx
@src/components/public/content/SeriesIndex.tsx
@src/components/public/content/SeriesDetailList.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement organization query helpers</name>
  <files>src/lib/public/content-queries.ts</files>
  <read_first>
    - `.planning/phases/04-public-content-library/04-CONTEXT.md`
    - `.planning/phases/04-public-content-library/04-RESEARCH.md`
    - `.planning/phases/04-public-content-library/04-PATTERNS.md`
    - `.planning/phases/04-public-content-library/04-04-SUMMARY.md`
    - `src/lib/public/content-queries.ts`
  </read_first>
  <behavior>
    - D-09: tag/category helpers use independent relation filters.
    - D-10: archive helper groups by non-null `publishedAt` year and month.
    - D-11: series index helper returns only series title/name and description data needed by the component.
    - D-12: series detail helper returns published posts by ascending `seriesOrder`.
    - TAX-05: every helper reuses the shared published-only predicate.
  </behavior>
  <action>Extend `content-queries.ts` with `getPublishedPostsByTag(slug)`, `getPublishedPostsByCategory(slug)`, `getPublishedArchiveGroups()`, `getPublishedSeriesIndex()`, and `getPublishedSeriesBySlug(slug)`. Every helper must reuse the shared published-only predicate and require non-null `publishedAt`; do not return draft-only taxonomy or series data to public routes.</action>
  <acceptance_criteria>
    - Public organization helpers all reuse the shared published-only predicate and require non-null `publishedAt`.
    - Tag and category helpers filter through their separate relations and do not share one ambiguous route shape.
    - Archive grouping is derived from published dates only.
    - Series detail ordering is ascending by `seriesOrder` and excludes unpublished posts.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>Organization query helpers are published-only and ready for route wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement tag, category, archive, series index, and series detail routes</name>
  <files>src/app/(public)/tags/[slug]/page.tsx, src/app/(public)/categories/[slug]/page.tsx, src/app/(public)/archive/page.tsx, src/app/(public)/series/page.tsx, src/app/(public)/series/[slug]/page.tsx</files>
  <read_first>
    - `.planning/phases/04-public-content-library/04-CONTEXT.md`
    - `.planning/phases/04-public-content-library/04-UI-SPEC.md`
    - `.planning/phases/04-public-content-library/04-PATTERNS.md`
    - `.planning/phases/04-public-content-library/04-04-SUMMARY.md`
    - `src/lib/public/content-queries.ts`
    - `src/app/(public)/archive/page.tsx`
    - `src/app/(public)/series/page.tsx`
    - `src/components/public/content/TaxonomyPageHeader.tsx`
    - `src/components/public/content/ArchiveTimeline.tsx`
    - `src/components/public/content/SeriesIndex.tsx`
    - `src/components/public/content/SeriesDetailList.tsx`
  </read_first>
  <behavior>
    - D-09: tag/category routes have independent path structures and semantic headings.
    - D-10: archive route renders year/month groups.
    - D-11: series index route renders only name/title and description, not counts or latest-update metadata.
    - D-12: series detail route renders ordered published posts.
    - D-19: empty organization pages use Chinese copy and no admin links.
  </behavior>
  <action>Replace inactive `/archive` and `/series` bodies, add dynamic tag/category/series pages, and wire them to the organization helpers and components from Plan 04-04. Organization detail routes should call `notFound()` when no published post is available for the slug. Do not query Prisma directly from route files.</action>
  <acceptance_criteria>
    - `/tags/[slug]` and `/categories/[slug]` are separate semantic routes and list only published posts.
    - `/archive` renders published posts grouped by year and month.
    - `/series` renders only series names/descriptions and `/series/[slug]` renders published posts by ascending `seriesOrder`.
    - Dynamic organization routes call `notFound()` for private-only or nonexistent slugs.
    - The RED organization browser coverage from Plan 04-04 passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint && npm run build && npx playwright test src/tests/e2e/public-content-library.spec.ts --project=desktop --project=mobile --project=min-mobile</automated>
  </verify>
  <done>Tag, category, archive, series index, and series detail routes are live and published-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> dynamic taxonomy slug | Untrusted slugs select public organization pages. |
| database -> taxonomy route | Taxonomy and series relations become public navigation. |
| database -> archive route | Publication dates become public chronology. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10-01 | Information Disclosure | taxonomy and series routes | high | mitigate | Return published-only posts and call `notFound()` for private-only slugs. |
| T-04-10-02 | Information Disclosure | archive route | high | mitigate | Archive helper reads only published posts with non-null `publishedAt`. |
| T-04-10-03 | Tampering | dynamic route slugs | medium | mitigate | Route params feed typed Prisma filters only through public helpers. |
| T-04-SC | Tampering | npm installs | high | mitigate | No package installation is authorized in this plan. |
</threat_model>

<verification>
Overall plan verification:
- `npm run lint`
- `npm run build`
- `npx playwright test src/tests/e2e/public-content-library.spec.ts --project=desktop --project=mobile --project=min-mobile`
</verification>

<success_criteria>
- ORG-01 through ORG-04 and TAX-05 are implemented with published-only filtering.
- D-09, D-10, D-11, D-12, D-19, and D-23 have route/browser coverage.
- Plans 04-05 and 04-07 can consume organization-route paths without also building organization routes.
</success_criteria>

<output>
Create `.planning/phases/04-public-content-library/04-10-SUMMARY.md` when done.
</output>
