---
phase: 05-interaction-polish-and-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/tests/e2e/admin-authoring.spec.ts
  - .planning/phases/03-markdown-authoring-workflow/03-VERIFICATION.md
autonomous: true
requirements:
  - QUAL-05
must_haves:
  truths:
    - "D-02 and D-04: browser evidence proves title-to-slug automation/manual override, excerpt storage, table storage, Markdown persistence, and editor-package isolation."
    - "D-09 and D-10: targeted authoring coverage and the complete lint/unit/build/browser/security gates pass on the current Tiptap implementation."
    - "D-16: Phase 3 verification records only fresh passing evidence and explains the legitimate UIW-to-Tiptap cutover instead of treating it as drift."
  artifacts:
    - path: "src/tests/e2e/admin-authoring.spec.ts"
      provides: "missing release-critical authoring behavior assertions"
    - path: ".planning/phases/03-markdown-authoring-workflow/03-VERIFICATION.md"
      provides: "fresh Phase 3 goal-backward verification record"
  key_links:
    - from: "src/tests/e2e/admin-authoring.spec.ts"
      to: "src/components/admin/PostEditorShell.tsx"
      via: "real title/slug/excerpt and toolbar browser interactions"
      pattern: "slug|excerpt|插入表格"
    - from: ".planning/phases/03-markdown-authoring-workflow/03-VERIFICATION.md"
      to: "src/tests/e2e/admin-authoring.spec.ts"
      via: "fresh command evidence and named behavior coverage"
      pattern: "Tiptap|QUAL-01|CMS"
---

## Phase Goal

**As a** site owner preparing the blog for public release, **I want to** verify the complete reader and administrator workflows across supported environments, **so that** the blog is release-ready with reproducible evidence and provider-neutral operating instructions.

<objective>
Close the narrow Phase 3 evidence gaps required by D-02, D-04, D-09, D-10, and D-16 without rebuilding or redesigning the authoring workflow.

Purpose: Turn already implemented authoring behavior into durable release evidence and reconcile the historical UIW plan with the accepted Tiptap canvas.
Output: Focused Playwright assertions and a fresh `03-VERIFICATION.md` created only after current gates pass.
</objective>

## Artifacts this phase produces

- New browser behaviors in `src/tests/e2e/admin-authoring.spec.ts`: title-derived slug until manual override, excerpt persistence, saved GFM table persistence/reopen, and no public `@tiptap/*` source reachability.
- New `.planning/phases/03-markdown-authoring-workflow/03-VERIFICATION.md`.
- No editor redesign, authoring feature, dependency, route, or product capability.

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-markdown-authoring-workflow/03-UAT.md
@.planning/phases/05-interaction-polish-and-verification/05-CONTEXT.md
@.planning/phases/05-interaction-polish-and-verification/05-PATTERNS.md
@.planning/quick/260711-fwf-admin-visual-security-hardening/260711-fwf-VERIFICATION.md
@src/tests/e2e/admin-authoring.spec.ts
@src/components/admin/PostEditorShell.tsx
@src/lib/admin/post-input.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the missing authoring behavior evidence</name>
  <files>src/tests/e2e/admin-authoring.spec.ts</files>
  <read_first>
    - `.planning/phases/03-markdown-authoring-workflow/03-UAT.md`
    - `.planning/phases/05-interaction-polish-and-verification/05-CONTEXT.md`
    - `.planning/phases/05-interaction-polish-and-verification/05-PATTERNS.md`
    - `src/tests/e2e/admin-authoring.spec.ts`
    - `src/components/admin/PostEditorShell.tsx`
    - `src/lib/admin/post-input.ts`
    - `src/components/admin/wysiwyg/AdminWysiwygEditorClient.tsx`
    - `src/lib/admin/wysiwyg/markdown-adapter.ts`
  </read_first>
  <behavior>
    - A new-post title updates the URL path automatically until the administrator edits the path, after which later title edits leave the manual path unchanged.
    - Create and edit saves persist the exact excerpt alongside title, status, cover, and Markdown body.
    - A table edited through the real canvas saves GFM table syntax containing the edited cell and reopens as a table with that value.
    - Public route, public component, and shared public Markdown source graphs contain no Tiptap import while protected admin code may import the pinned editor.
  </behavior>
  <action>Extend the existing serial suite and its Prisma helpers rather than adding a second fixture system. Add one focused title/slug automation test; strengthen the existing create/edit database assertions with exact excerpt values; complete the existing table edit test by assigning a unique title/path, saving, querying `bodyMarkdown` for GFM table structure and `alpha`, then reopening the same post; and extend the current source-graph test to reject every `@tiptap/*` import from public routes/components/shared public Markdown. Preserve the existing UIW absence assertion and current toolbar/code/image coverage. Per D-04 and D-08, test the selected Tiptap design as implemented and make no visual redesign.</action>
  <acceptance_criteria>
    - Slug generation and the manual-override stop condition are both asserted through visible controls.
    - Database assertions cover excerpt values after create and edit.
    - Table persistence is proven after save and reopen, not only inside unsaved editor state.
    - Public source reachability rejects legacy UIW and all Tiptap editor imports while admin imports remain allowed.
  </acceptance_criteria>
  <verify>
    <automated>xvfb-run -a npx playwright test src/tests/e2e/admin-authoring.spec.ts --project=desktop --headed</automated>
  </verify>
  <done>Every genuine Phase 3 release-evidence gap has a passing behavior assertion against the current editor.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify Phase 3 and record the accepted editor cutover</name>
  <files>.planning/phases/03-markdown-authoring-workflow/03-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/03-markdown-authoring-workflow/03-UAT.md`
    - `.planning/phases/03-markdown-authoring-workflow/03-01-SUMMARY.md`
    - `.planning/phases/03-markdown-authoring-workflow/03-02-SUMMARY.md`
    - `.planning/phases/03-markdown-authoring-workflow/03-03-SUMMARY.md`
    - `.planning/STATE.md`
    - `src/tests/e2e/admin-authoring.spec.ts`
    - `package.json`
  </read_first>
  <action>Run the current targeted suite, `verify:ci`, the complete four-project Playwright matrix in headed Xvfb mode, and the production security smoke. Only after all mandatory commands pass, create `03-VERIFICATION.md` with goal-backward truth/artifact/link evidence for Phase 3 requirements, the actual command outputs from this run, and no recalled test counts. Explicitly record that the UIW source/preview path in 03-02 is historical and was intentionally superseded by the accepted Tiptap Markdown-backed WYSIWYG cutover; verify public bundle/source isolation against the active architecture. If a mandatory command fails, do not mark the artifact passed. This enforces D-09, D-10, and D-16.</action>
  <acceptance_criteria>
    - The verification artifact is created only after all named current gates pass.
    - Evidence maps authoring, validation, taxonomy, Markdown safety, persistence, and editor isolation to current files/tests.
    - The UIW-to-Tiptap cutover is recorded as an accepted implementation change, not an unresolved discrepancy.
    - Recorded counts and results come from this execution.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:ci && xvfb-run -a npm run test:e2e -- --headed && xvfb-run -a npm run security:smoke && test -f .planning/phases/03-markdown-authoring-workflow/03-VERIFICATION.md</automated>
  </verify>
  <done>Phase 3 has current, auditable verification evidence with no unresolved behavior gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser input -> persisted Post | Slug, excerpt, table content, and Markdown cross into PostgreSQL. |
| admin-only editor packages -> public source graph | Heavy privileged authoring code must not become public-route reachable. |
| prior planning evidence -> release decision | Historical UIW evidence could produce a false pass against the current editor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Tampering | authoring persistence evidence | medium | mitigate | Query saved rows and reopen the same table post rather than trusting unsaved DOM state. |
| T-05-02-02 | Information Disclosure | public source graph | medium | mitigate | Scan the complete public route/component/Markdown graph for all active and legacy editor packages. |
| T-05-02-03 | Repudiation | Phase 3 verification record | medium | mitigate | Record fresh commands/results and the explicit accepted cutover, and refuse passed status on a failed gate. |
| T-05-SC | Tampering | npm installs | high | mitigate | No package installation or dependency change is authorized in this plan. |
</threat_model>

<verification>
Overall plan verification:
- `xvfb-run -a npx playwright test src/tests/e2e/admin-authoring.spec.ts --project=desktop --headed`
- `npm run verify:ci`
- `xvfb-run -a npm run test:e2e -- --headed`
- `xvfb-run -a npm run security:smoke`
</verification>

<success_criteria>
- D-02 and D-04 authoring behavior is supported by persisted browser evidence.
- D-09 and D-10 gates pass against the current application.
- D-16 permits Phase 3 verification to pass with the deliberate UIW-to-Tiptap cutover clearly reconciled.
</success_criteria>

<output>
Create `.planning/phases/05-interaction-polish-and-verification/05-02-SUMMARY.md` when done.
</output>
