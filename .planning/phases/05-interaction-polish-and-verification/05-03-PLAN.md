---
phase: 05-interaction-polish-and-verification
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .env.example
  - README.md
  - docs/operations/postgres-backup-restore.md
autonomous: true
requirements:
  - QUAL-05
must_haves:
  truths:
    - "D-13 and D-14: an operator can configure, migrate, bootstrap, build, start, probe, verify, and roll back the blog without choosing a hosting provider."
    - "Production guidance requires a final HTTPS ADMIN_SITE_ORIGIN and clearly separates runtime secrets from the Playwright-only password."
    - "A provider-neutral PostgreSQL backup and isolated restore drill protects content and records reproducible recovery evidence."
    - "D-15 and D-16: operations docs preserve the existing deferred scope and never claim repository checks complete the external provider gates."
  artifacts:
    - path: ".env.example"
      provides: "runtime/test environment boundary and production origin requirement"
    - path: "README.md"
      provides: "authoritative setup, release, health, verification, rollback, and security entry point"
    - path: "docs/operations/postgres-backup-restore.md"
      provides: "provider-neutral backup, restore, validation, and evidence runbook"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "exact package script names established by 05-01"
      pattern: "db:migrate:deploy|verify:ci|security:smoke"
    - from: "README.md"
      to: "docs/operations/postgres-backup-restore.md"
      via: "production data protection and rollback guidance"
      pattern: "postgres-backup-restore"
    - from: ".env.example"
      to: "src/lib/auth/env.ts"
      via: "documented names match runtime validation"
      pattern: "ADMIN_SITE_ORIGIN|ADMIN_SESSION_SECRET"
---

## Phase Goal

**As a** site owner preparing the blog for public release, **I want to** verify the complete reader and administrator workflows across supported environments, **so that** the blog is release-ready with reproducible evidence and provider-neutral operating instructions.

<objective>
Close the operator-facing repository gaps required by D-13, D-14, D-15, and D-16 while leaving provider selection, domains, and secret values external.

Purpose: Make setup, production start, rollback, and PostgreSQL recovery repeatable from current repository truth.
Output: A structured environment example, root README, and backup/restore drill runbook.
</objective>

## Artifacts this phase produces

- Updated `.env.example` with separate runtime and Playwright-only sections.
- New root `README.md` covering Node 22/npm, PostgreSQL, environment setup, migration, bootstrap, development, verification, production start, health, rollback, and security launch gates.
- New `docs/operations/postgres-backup-restore.md` with logical backup and isolated restore drill procedures.
- No provider config, domain, credential, media service, deployment action, or deferred product feature.

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-interaction-polish-and-verification/05-CONTEXT.md
@.planning/phases/05-interaction-polish-and-verification/05-PATTERNS.md
@docs/security/public-exposure-audit-2026-07-11.md
@package.json
@.env.example
@prisma.config.ts
@playwright.config.ts
@src/lib/auth/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the complete setup, production, verification, and rollback path</name>
  <files>.env.example, README.md</files>
  <read_first>
    - `.planning/PROJECT.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/phases/05-interaction-polish-and-verification/05-CONTEXT.md`
    - `.planning/phases/05-interaction-polish-and-verification/05-PATTERNS.md`
    - `package.json`
    - `.env.example`
    - `prisma.config.ts`
    - `playwright.config.ts`
    - `src/lib/auth/env.ts`
    - `docs/security/public-exposure-audit-2026-07-11.md`
  </read_first>
  <action>Reorganize `.env.example` into production runtime and local/CI browser-test sections while keeping every value blank. Mark `DATABASE_URL`, `ADMIN_EMAIL`, `ADMIN_PASSWORD_HASH`, `ADMIN_SESSION_SECRET`, and a final absolute HTTPS `ADMIN_SITE_ORIGIN` as production requirements; identify `PLAYWRIGHT_ADMIN_PASSWORD` as test-only. Write `README.md` from current package/config truth: Node 22 and npm prerequisites, PostgreSQL, install, local env, `db:migrate:deploy`, password hashing, idempotent admin bootstrap, development, `verify`, headed browser verification, build/start, liveness/readiness semantics, deployment smoke, application rollback, migration forward-fix/backup restore policy, and links to the security audit and recovery runbook. Per D-14, state that TLS, fixed Host/origin, secret injection, private TLS database, WAF/proxy limits, monitoring, restore drill, and independent infrastructure testing remain external launch gates. Per D-15, list existing deferrals only as scope boundaries and do not create a new roadmap.</action>
  <acceptance_criteria>
    - Environment comments distinguish production runtime values from the browser-test-only password and contain no value.
    - README commands exactly match package scripts and describe migration-before-bootstrap-before-build/start ordering.
    - README documents both health endpoints and does not treat liveness as database readiness.
    - Rollback guidance protects database compatibility and links to the recovery runbook.
    - Provider controls are explicit external blockers rather than silently assumed complete.
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e 'import fs from "node:fs"; const r=fs.readFileSync("README.md","utf8"); for (const token of ["db:migrate:deploy","admin:bootstrap","verify:ci","security:smoke","/api/health","/api/health/ready","postgres-backup-restore","public-exposure-audit-2026-07-11"]) if (!r.includes(token)) throw new Error(`README missing ${token}`);' && npm run security:scan</automated>
  </verify>
  <done>A clean operator can follow one current, secret-free path from checkout through local use and production preparation.</done>
</task>

<task type="auto">
  <name>Task 2: Add a provider-neutral PostgreSQL backup and recovery drill</name>
  <files>docs/operations/postgres-backup-restore.md</files>
  <read_first>
    - `.planning/phases/05-interaction-polish-and-verification/05-CONTEXT.md`
    - `docs/security/public-exposure-audit-2026-07-11.md`
    - `README.md`
    - `prisma/schema.prisma`
    - `prisma.config.ts`
    - `package.json`
  </read_first>
  <action>Write a PostgreSQL runbook using placeholders for host, port, database, user, and encrypted output location. Cover TLS and least-privilege prerequisites; `pg_dump` logical backup; encrypted off-host storage, retention, access, and integrity metadata; restore into a newly isolated database; `db:migrate:deploy`, Prisma validation, liveness/readiness, public read, and administrator login smoke checks; evidence fields for timestamp/operator/source/target/checksum/result; restore cadence; failure handling; and explicit cleanup of the isolated target. Distinguish application rollback from data restore, require a verified backup before destructive recovery, and prefer migration forward-fix unless an approved restore is required. Never include a live URL, credential, provider CLI, or claim that writing the runbook completes the D-14 restore gate.</action>
  <acceptance_criteria>
    - Backup steps use standard PostgreSQL tooling, TLS, least privilege, encryption, checksum, retention, and off-host storage concepts.
    - Restore always targets an isolated database first and includes schema/application smoke verification.
    - The runbook has auditable evidence fields, cadence, cleanup, failure, and escalation procedures.
    - No production secret, authenticated URL, provider-only command, or destructive unverified restore appears.
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e 'import fs from "node:fs"; const s=fs.readFileSync("docs/operations/postgres-backup-restore.md","utf8"); for (const token of ["pg_dump","pg_restore","TLS","checksum","db:migrate:deploy","/api/health/ready"]) if (!s.includes(token)) throw new Error(`runbook missing ${token}`);' && npm run security:scan && git diff --check</automated>
  </verify>
  <done>The repository contains a safe, repeatable recovery drill that can produce the provider-gate evidence required before public exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator environment -> application runtime | Database/auth/origin values are injected outside source control. |
| production PostgreSQL -> backup storage | Complete blog/admin/session data leaves the live database boundary. |
| backup artifact -> isolated restore target | Archived data is trusted only after integrity and recovery checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | environment/docs | high | mitigate | Keep example values blank, label test-only input, link secret-manager requirements, and run the source scanner. |
| T-05-03-02 | Information Disclosure | database backups | high | mitigate | Require TLS, least privilege, encryption, restricted off-host storage, retention, and access evidence. |
| T-05-03-03 | Tampering | backup integrity | high | mitigate | Record checksum and source metadata before accepting an artifact for recovery. |
| T-05-03-04 | Denial of Service | restore/rollback | medium | mitigate | Restore into isolation, verify compatibility and smoke gates, and forbid unverified destructive recovery. |
| T-05-SC | Tampering | npm installs | high | mitigate | No package installation or dependency change is authorized in this plan. |
</threat_model>

<verification>
Overall plan verification:
- README command/link contract checks.
- Recovery-runbook content contract checks.
- `npm run security:scan`
- `git diff --check`
</verification>

<success_criteria>
- D-13 repository operations are reproducible and auditable.
- D-14 external launch gates are explicit and provider-neutral.
- D-15 deferred product scope is preserved without implementation or roadmap expansion.
- D-16 release readiness no longer lacks onboarding or recovery instructions.
</success_criteria>

<output>
Create `.planning/phases/05-interaction-polish-and-verification/05-03-SUMMARY.md` when done.
</output>
